% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_input_dot_project_files.R
\name{generate_pre_prompt}
\alias{generate_pre_prompt}
\title{Generate a formatted prompt string with rules and optional headers/footers}
\usage{
generate_pre_prompt(
  vector_rules,
  prefixes = "Follow these rules",
  postfixes = "R code below:",
  list_item = " - ",
  collapse_char = "\\n"
)
}
\arguments{
\item{vector_rules}{A character vector of rule strings to include in the body of the prompt.}

\item{prefixes}{A character vector or single string to prepend before the rules (default: 'Follow these rules').}

\item{postfixes}{A character vector or single string to append after the rules (default: 'R code below:').}

\item{list_item}{A string used to prefix each rule item (default: ' - ').}

\item{collapse_char}{Collapse the different lines together (default : '\\n').}
}
\value{
A single character string containing the formatted prompt.
}
\description{
This function constructs a formatted text prompt by combining a prefix (e.g., instructions),
a list of rules (each optionally prefixed with a list item marker), and a postfix (e.g., 
a lead-in for code). Each component is joined with newline characters for readability.
}
\examples{
generate_pre_prompt(
  vector_rules = c("Do not use external libraries", "Use vectorized operations"),
  prefixes = "Please follow these coding rules:",
  postfixes = "Here is the R code:"
)
}
